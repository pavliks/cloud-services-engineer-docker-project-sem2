services:
  backend:
    image: momo-store-backend
    build:
      context: ./backend
    expose:
      - "8081"
  frontend:
    image: momo-store-frontend
    build:
      context: ./frontend
    ports:
      - "80:8080"
